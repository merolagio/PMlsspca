% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotcor.R
\name{plotcor}
\alias{plotcor}
\title{Correlation heatmap with optional grouping and annotations}
\usage{
plotcor(
  S,
  axis_labels = TRUE,
  groups = FALSE,
  subgroups = FALSE,
  separate_groups = (!(groups[[1]] == FALSE))[1],
  group_sepline = "black",
  bnw = FALSE,
  addlabels = FALSE,
  thresholdFontColLabels = 0.33,
  label_size = 5,
  label_face = "plain",
  lowcol = c("firebrick", "darkgreen"),
  highcol = c("dodgerblue3", "darkblue"),
  axis_lab_size = 10,
  bottom_axis_labels = FALSE,
  side_axis_labels = TRUE,
  add_group_names = F,
  group_names = NULL,
  group_names_size = 4,
  group_names_face = "plain",
  group_names_vjust = 0,
  expandTop = c(0, 0.1),
  clipPlotOff = FALSE,
  subgroup_sepline_colour = "black",
  subgroup_sepline_size = 1,
  plt = TRUE,
  rtn_plot = FALSE
)
}
\arguments{
\item{S}{A square numeric matrix (typically a correlation matrix) with values in \eqn{[-1, 1]}. Rows/columns are reordered if `groups` is provided.}

\item{axis_labels}{Controls axis labels. `TRUE` uses column/row names if available (otherwise uses `V1, \dots, Vp`); `FALSE` or `"none"` removes labels. A character/factor vector can be supplied to use custom labels.}

\item{groups}{Optional grouping information used to reorder variables and (optionally) draw separators. Accepted inputs: `FALSE` (no grouping), a permutation vector of indices `1:p`, a character/factor vector of length `p` (group labels), or a list of
integer vectors (one per group).}

\item{subgroups}{Optional subgroup specification (same accepted formats as `groups`). When provided and groups are separated, subgroup rectangles are drawn.}

\item{separate_groups}{Logical or numeric. If positive, draws separators between groups with the specified line width.}

\item{group_sepline}{Color for group separator lines.}

\item{bnw}{Logical. If `TRUE`, uses a black/white fill scale; otherwise uses a diverging scale.}

\item{addlabels}{Logical. If `TRUE`, overlays numeric correlation labels inside cells.}

\item{thresholdFontColLabels}{Numeric. Threshold used to choose black/white text for overlaid labels (relative to the displayed values).}

\item{label_size}{Numeric. Size of overlaid labels.}

\item{label_face}{Character. Font face for overlaid labels (e.g., `"plain"`, `"bold"`).}

\item{lowcol, highcol}{Length-2 character vectors. Colors used for the diverging fill scale (only the first element is used).}

\item{axis_lab_size}{Numeric. Axis label text size.}

\item{bottom_axis_labels, side_axis_labels}{Logical. Whether to show x- and y-axis labels.}

\item{add_group_names}{Logical. If `TRUE`, adds group names above the plot (requires `separate_groups > 0` and grouped input).}

\item{group_names}{Optional character vector of group names (length must match the number of groups). If `NULL` and groups are named, names are used.}

\item{group_names_size}{Numeric. Size of group name labels.}

\item{group_names_face}{Character. Font face for group name labels.}

\item{group_names_vjust}{Numeric. Vertical adjustment for group name labels.}

\item{expandTop}{Numeric vector. Expansion applied to the top of the plot when adding group names (passed to `scale_y_discrete(expand = expansion(...))`).}

\item{clipPlotOff}{Logical. If `TRUE`, sets `coord_cartesian(clip = "off")` to allow group names to extend beyond the panel.}

\item{subgroup_sepline_colour}{Color for subgroup rectangle outlines.}

\item{subgroup_sepline_size}{Numeric. Line width for subgroup rectangle outlines.}

\item{plt}{Logical. If `TRUE`, prints the plot.}

\item{rtn_plot}{Logical. If `TRUE`, returns the `ggplot` object.}
}
\value{
If `rtn_plot = TRUE`, a `ggplot` object; otherwise `NULL` (invisibly). If`plt = TRUE`, the plot is printed.
}
\description{
Draws a correlation heatmap from a square matrix `S`, optionally reordering variables
by groups, adding block separators (and subgroup rectangles), and overlaying numeric correlation labels.
}
\seealso{
\code{\link[ggplot2]{ggplot}}, \code{\link[reshape2]{melt}}
}
