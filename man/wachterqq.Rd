% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Utilities.R
\name{wachterqq}
\alias{wachterqq}
\title{Wachter (Marchenko--Pastur) QQ plot for eigenvalues}
\usage{
wachterqq(
  eigvals,
  p = NULL,
  n,
  gamma,
  cor = T,
  nplot,
  nfit_line = NULL,
  addtitle = TRUE,
  prn = TRUE,
  rtn = FALSE
)
}
\arguments{
\item{eigvals}{Numeric vector of eigenvalues (assumed sorted decreasing).}

\item{p}{Integer. Number of variables.}

\item{n}{Integer. Sample size.}

\item{gamma}{Numeric. Aspect ratio; defaults to `n/p` if missing.}

\item{cor}{Logical. If `TRUE`, rescales MP quantiles to match a correlation
matrix trace (sum to `p`).}

\item{nplot}{Integer. Number of leading eigenvalues to include; defaults to
`length(eigvals)`.}

\item{nfit_line}{Integer or `NULL`. If positive, fits an `lm` line using the
last `nfit_line` points; if negative, excludes the nfit largest values.}

\item{addtitle}{Logical. If `TRUE`, adds a plot title.}

\item{prn}{Logical. If `TRUE`, prints the plot.}

\item{rtn}{Logical. If `TRUE`, returns the ggplot object.}
}
\value{
If `rtn = TRUE`, a `ggplot` object; otherwise `NULL` (invisibly).
}
\description{
Produces a QQ-plot comparing observed eigenvalues to Marchenko--Pastur
(Wachter) theoretical quantiles for aspect ratio \eqn{\gamma = n/p}.
}
\examples{
# wachterqq(eigvals, p = ncol(X), n = nrow(X), cor = TRUE, nfit_line = 5, rtn = TRUE)

}
